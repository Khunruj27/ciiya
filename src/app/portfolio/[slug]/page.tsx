// The only public Portfolio URL. The editor remains at the parent
// /portfolio route while this dynamic child renders a saved Portfolio.
export {
  generateMetadata,
  revalidate,
} from '@/components/public-portfolio-page'
export { default } from '@/components/public-portfolio-page'
